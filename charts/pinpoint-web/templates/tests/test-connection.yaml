apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pinpoint-web.fullname" . }}-test-connection"
  labels:
{{ include "pinpoint-web.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pinpoint-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
